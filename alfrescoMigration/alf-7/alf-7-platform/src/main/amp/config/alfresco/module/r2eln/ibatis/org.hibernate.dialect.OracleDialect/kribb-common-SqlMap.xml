<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kribb.common">
	<select id="selectPerson" parameterType="string" resultType="hmap">
		SELECT 
			PAYNUM, NAME, BUSO, EMAIL_ADDR, BUSONAME, GUBTAE_NM, TENURE_YN, PASSWD 
		FROM
			I_V_ELN_INSA
		WHERE
			PAYNUM=#{value}
	</select>
</mapper>