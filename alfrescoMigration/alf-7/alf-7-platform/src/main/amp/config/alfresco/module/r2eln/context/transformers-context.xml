<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE beans PUBLIC '-//SPRING//DTD BEAN//EN' 'http://www.springframework.org/dtd/spring-beans.dtd'>
<beans>
    <!-- The PDF transformer -->
<!--     <bean id="transformer.eln4pdf" class="com.argo.r2eln.repo.transform.UniflowPdfTransformer" parent="baseContentTransformer">
          <property name="sourceDir" value="${transformer.eln4pdf.source.dir}"/>
          <property name="convertDir" value="${transformer.eln4pdf.convert.dir}"/>
          <property name="avaliable" value="${transformer.eln4pdf.avaliable}"/>
          <property name="doDeletion" value="${transformer.eln4pdf.doDeletion}"/>
          <property name="serverPath" value="${transformer.eln4pdf.server.path}"/>
	      <property name="serverIP" value="${pdf.host.address}"/>
	      <property name="serverPort" value="${pdf.host.port}"/>
    </bean> -->
 
 
<!-- KISTI --> 
     <bean id="transformer.eln4pdf" class="com.argo.r2eln.repo.transform.EpapyrusPdfTransformer" parent="baseContentTransformer">
          <property name="sourceDir" value="${transformer.eln4pdf.source.dir}"/>
          <property name="convertDir" value="${transformer.eln4pdf.convert.dir}"/>
          <property name="avaliable" value="${transformer.eln4pdf.avaliable}"/>
          <property name="doDeletion" value="${transformer.eln4pdf.doDeletion}"/>
          <property name="convpdf" value="${transformer.eln4pdf.url}" />
    </bean>
 
 <!-- KRIBB -->
<!--    <bean id="transformer.eln4pdf" class="com.argo.r2eln.repo.transform.Uniflow2PdfTransformer" parent="baseContentTransformer"
    	init-method="init">
          <property name="sourceDir" value="${transformer.eln4pdf.source.dir}"/>
          <property name="convertDir" value="${transformer.eln4pdf.convert.dir}"/>
          <property name="remoteDir" value="${transformer.eln4pdf.remote.dir}"/>
	      <property name="serverIP" value="${pdf.host.address}"/>
	      <property name="serverPort" value="${pdf.host.port}"/>
          <property name="avaliable" value="${transformer.eln4pdf.avaliable}"/>
          <property name="doDeletion" value="${transformer.eln4pdf.doDeletion}"/>
    </bean> -->
<!-- samba로 연결한 argonet PDF : DGIST
     <bean id="transformer.eln4pdf" class="com.argo.r2eln.repo.transform.ArgonetPdfTransformer" parent="baseContentTransformer">
          <property name="sourceDir" value="${transformer.eln4pdf.source.dir}"/>
          <property name="convertDir" value="${transformer.eln4pdf.convert.dir}"/>
          <property name="avaliable" value="${transformer.eln4pdf.avaliable}"/>
          <property name="doDeletion" value="${transformer.eln4pdf.doDeletion}"/>
    </bean>  
-->    
<!-- samba 없이 연결한 argonet PDF : DEMO, RDA     
	<bean id="transformer.eln4pdf" class="com.argo.r2eln.repo.transform.Argonet2PdfTransformer" parent="baseContentTransformer">
          <property name="sourceDir" value="${transformer.eln4pdf.source.dir}"/>
          <property name="convertDir" value="${transformer.eln4pdf.convert.dir}"/>
          <property name="avaliable" value="${transformer.eln4pdf.avaliable}"/>
          <property name="doDeletion" value="${transformer.eln4pdf.doDeletion}"/>
    </bean> -->  
</beans>